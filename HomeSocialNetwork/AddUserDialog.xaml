<Window x:Class="HomeSocialNetwork.AddUserDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HomeSocialNetwork"
        mc:Ignorable="d"
        Title="Добавить пользователя" Height="250" Width="400"
        WindowStartupLocation="CenterOwner">

    
<Grid Margin="16">
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>  
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="140"/>
        <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>

        <!-- Email -->
        <Label Grid.Row="0" Grid.Column="0" Content="Email:"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="EmailTextBox" />

        <!-- Пароль -->
        <Label Grid.Row="1" Grid.Column="0" Content="Enter Пароль:"/>
        <PasswordBox Grid.Row="1" Grid.Column="1" x:Name="PasswordBox" />

        <!-- Кнопки -->
        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Content="ОК" Width="70" Click="OkButton_Click" IsDefault="True" Margin="0 0 10 0"/>
            <Button Content="Отмена" Width="70" Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>

